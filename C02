#include <stdio.h>
#include <math.h>

int main() {
    int n;
    scanf("%d", &n);
    double S = 0;
    for (int i = 1; i <= n; ++i) {
        S += 1.0 / (i*i);
    }
    double pi = pow(S * 32, 1.0 / 3);
    printf("%.5f\n", pi);
    return 0;
}
